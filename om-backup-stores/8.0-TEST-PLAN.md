# OM Backup Phase 1 — v8.0 backport local re-verification plan

Re-run the rollback-impact PoC against the **v8.0 backport** of OM Backup
Phase 1 to confirm the same protections we verified on `main` are still in
place after the cherry-picks. Reference baseline: [`RESTORE-FINDINGS.md`](RESTORE-FINDINGS.md)
(main) and [`SCENARIO-1-FIX-PROPOSAL.md`](SCENARIO-1-FIX-PROPOSAL.md) (the
`#770` snapshot-block validator, also in the v8.0 backport).

---

## Branches under test

| Repo | Branch | Base | Notes |
|---|---|---|---|
| `10gen/ops-manager` | [`prakhar.dhama/om-backup-local-8.0`](https://github.com/10gen/ops-manager/tree/prakhar.dhama/om-backup-local-8.0) | PR [#862](https://github.com/10gen/ops-manager/pull/862) head (= `v8.0` + OM Backup Phase 1 backport) | Adds the 4 PoC-only conf/BUILD tweaks from `prakhar.dhama/om-backup-local`, drops `testGet_featureFlagDisabled_restorationModeIgnored`, and flips `mms.featureFlag.automation.restorationMode=enabled` in all 7 conf files |
| `10gen/mms-automation` | [`prakhar.dhama/om-backup-local-8.0`](https://github.com/10gen/mms-automation/tree/prakhar.dhama/om-backup-local-8.0) | `origin/ops-manager-8.0` | Adds local agent configs (`local.config`, `local2.config`) + flips `OmBackupFeatureFlag` default to `true` in `global.go` and `flags.go`. Skips the new DR-fallback unit tests (they use the 5-arg `ReadClusterConfig` signature that v8.0 doesn't have) — production code path still carries the DR mock fix |
| `ankitgarg-mongodb/mongo-infra-docker` (pdfork: `prakhardhama/mongo-infra-docker`) | `prakhar.dhama/om-backup-local` | (this repo) | Same scripts; this doc tracks the 8.0-specific run |

> Implementation note on the omitted unit tests: PR #862 brought the
> production-code side of CLOUDP-388021 (the `ReadClusterConfig` DR-fallback
> mock fix in `urlretriever.go`). The added `TestReadClusterConfigWithStandbyModifications`
> + `TestReadClusterConfigShortCircuit…` tests in `config_int_test.go` on
> `main` use a 5-arg `ReadClusterConfig` signature that doesn't exist on
> v8.0 (`JsonPatchSupplier` argument was added later). Carrying them over
> would mean either stripping the 3rd arg from ~6 call sites (mechanical but
> noisy) or back-importing the signature change. For E2E re-verification
> they're not load-bearing, so they're skipped.

---

## Build & run

### Primary OM (ops-manager v8.0 branch)

```bash
cd ~/ops-manager
git fetch origin prakhar.dhama/om-backup-local-8.0
git checkout prakhar.dhama/om-backup-local-8.0

# Same launch as main — the conf-hosted flip is baked into the branch
bazel run --server_env=hosted //server:mms
```

`mms.featureFlag.automation.restorationMode` is already `enabled` in
`conf-hosted.properties` on this branch, so no `--jvm_flag` override needed.

### Agent (mms-automation ops-manager-8.0 branch)

```bash
cd ~/mms-automation
git fetch origin prakhar.dhama/om-backup-local-8.0
git checkout prakhar.dhama/om-backup-local-8.0

# Same launch as main — OmBackupFeatureFlag default is now true on this branch
bazel run go_planner/src/com.tengen/cm/main:agent -- \
  -f go_planner/src/com.tengen/cm/main/local.config
```

For the second agent (Two-agent setup), use `local2.config`. Both configs
match what's on `prakhar.dhama/om-backup-local`.

### Backing DBs + Meta OM

No change — same `0-setup-backing-dbs.sh` / `3-verify-setup.sh` flow from
the main run. The docker compose stack is repo-independent.

---

## Scope of re-verification on 8.0

We're not re-running the entire 3-scenario matrix. The goal is to verify
that the **load-bearing safety net** (PR #770 snapshot-block validation +
PR #754 restoration-mode UI/API guardrails + PR #752 retry/recovery) made
it intact through the v8.0 backport. The 3 backing-store rollback shapes
are tested by the agent-side cv-polling path, the OM-side restoration-mode
state machine, and the daemon-side block-fileId pre-check — all of which
landed in PR #862.

### Tier 1 — Must pass

| # | Probe | Source | Expected on 8.0 | Why |
|---|---|---|---|---|
| 1 | Healthy snapshot restore (clean S3 path) | RESTORE-FINDINGS S1 Run 2 / Probe 1 | ✅ FINISHED in ~2 min, `pull_count=3` | Sanity — confirms the build runs at all and Primary OM ↔ daemon ↔ s3-meta plumbing is intact |
| 2 | Broken snapshot restore (s3-meta rolled back past block-registration) | SCENARIO-1-FIX-PROPOSAL Test Run 1 / Probes 1–4 | ❌ Rejected with **HTTP 409 `SNAPSHOT_BLOCKS_MISSING`** at REST submit; no `backupRestoreUrl*` directive on automation config; mongods stay UP | Validates PR #770 (CLOUDP-405628) — the wipe-then-fail-loop bug closed on main is still closed on 8.0 |
| 3 | PITR-from-RS to a target whose auto-picked starter is broken | SCENARIO-1-FIX-PROPOSAL Test Run 1 / Probe 6 | ❌ Rejected with **HTTP 409 `SNAPSHOT_BLOCKS_MISSING`** + no directive write | Validates the PITR resolution path (commit `80363209433`) — closes the gap reported in S1 Run 2 / Probe 5 of `RESTORE-FINDINGS` |
| 4 | Restoration-mode enter/exit via API | Restoration Mode PR #424 (CLOUDP-388025) | `restorationMode=true` writes succeed; subsequent automation config PUT during restoration is rejected by `ETagFilter` | Validates Track 2 (Restoration Mode Core) — gate is active on 8.0 |
| 5 | Feature flag disabled — verify safety net is opt-in | `controlled`/`disabled` value in `conf-local` reverted manually | All Tier-1 protections (Probes 2, 3, 4) become no-ops; existing pre-#770 buggy behavior re-emerges | Confirms `AUTOMATION_RESTORATION_MODE` gates the new behavior at group level on 8.0 the same way it does on main |

### Tier 2 — Should pass (lower risk because no v8.0-specific change touches them)

| # | Probe | Source | Expected on 8.0 |
|---|---|---|---|
| 6 | oplog-meta-rs rollback → PITR picker rejects in-gap target | RESTORE-FINDINGS S2 Run 2 Probe 2 | Rejected at picker, no restore job created |
| 7 | appdb-rs rollback → post-rollback snapshots return 404 | RESTORE-FINDINGS S3 Run 2 Probe 2 | HTTP 404, no late-failure wipe |
| 8 | Automation-config encryption at rest (`EnableConfigEncryption=true` default) | Implementation Plan §encryption | Config docs stored encrypted in `automationcore.config`; agent decrypts on retrieval |
| 9 | Reconciliation engine — TTL-based eviction of config map entries | PR #445 (CLOUDP-388026) | After 2× TTL, no stale config-map entries; reconciliation triggers fire on schedule |

### Tier 3 — Skipped on 8.0 (not v8.0-specific risks)

- Concurrent-load rollback (rollback firing during snapshot upload). Same gap as main run; if covered later, run on `main` first, then mirror on 8.0.
- Scenarios 4–7 from `RESTORE-FINDINGS` "Future scenarios" — coordinated-rollback / mismatched-timestamps. Same as above.

---

## Known divergences from `prakhar.dhama/om-backup-local` (main-based)

These are intentional and should not affect test outcomes:

1. **No `2191eeab5ea` (Morphia `.sparse()` removal on `Group.RESTORATION_MODE_FIELD` index).** Already merged via PR #424 cherry-pick (in PR #862); cherry-pick on this branch came back empty.
2. **No `3ef0f4e5202` (Morphia import cleanup in `RestorationModeMetadata.java`).** Same — already in PR #862.
3. **No new `config_int_test.go` tests from `ad266a71de`.** See build-and-run note above. Skipped for signature drift; production-code DR mock fix is present.
4. **No `4d61cd1bdc` cv-sentinel review fix.** Target test code from `ad266a71de` isn't on this branch, so the review fix has no patch target.

If any Tier-1 probe fails on 8.0 but passes on `prakhar.dhama/om-backup-local`,
suspect drift between the v8.0 backport (PR #862) and the squashed-merge
versions of #424 / #445 / #555 / #554 / #584 / #578 / #752 / #754 / #770 on
main. Compare `git diff origin/main...origin/v8.0 -- <changed-file>` for the
relevant file.

---

## Result log

_Capture per-tier outcomes here as you run. Mirror the table style of
[`RESTORE-FINDINGS.md` § "Probe results"](RESTORE-FINDINGS.md#probe-results)
for diff-ability._

### Run 1 — 2026-05-29 (local bazel, single-host)

**Setup:** Primary OM on `om-backup-local-8.0` (commit `4c3e3fd5c7e`, JVM started with `OM_DONT_RUN_MIGRATIONS=1`, `-Dmms.featureFlag.automation.restorationMode=enabled`). Agent on mms-automation `om-backup-local-8.0`, reporting to "OM Backup" project (`69ef3ca159819e73bb7f8552`), 5 poRepSet members (1–4 + 10, the latter added pre-rollback to exercise the restoration-mode trigger) + 1 sharded cluster `poShardClust` (2 shards + 1 config server).

#### Tier 1

| # | Probe | Submitted | Status | Downloads | Actual |
|---|---|---|---|---|---|
| 1 | Healthy snapshot restore (poRepSet → 13:09 local) | 19:44 IST | ✅ **Finished** | 3 (clean path) | ✅ **PASS** — all 5 members converged through `RestoreRsMemberFromBackupPhase3` → `MarkRestoreFinished` in ~3s agent-side; OM `RestoreResource.finishParallelRestore` fired cleanly at `14:14:28Z`; post-restore `automation.config` shows no `backupRestoreUrl*` directives (only config knobs `backupRestoreIsS3DirectRestore` + `backupRestoreS3DirectRestoreNumWorkers`); config version bumped 109 → 113 across Phase 1 + Phase 2 |
| 2 | Broken-snapshot rejected at REST | _TBD_ | _TBD_ | n/a | _TBD_ |
| 3 | PITR-from-RS broken starter rejected | _TBD_ | _TBD_ | n/a | _TBD_ |
| 4 | Restoration-mode auto-enter via regression + auto-exit via reconciliation (appdb rollback to 13:32) | 14:05 UTC | ✅ **Banner shown then auto-cleared** | n/a | ✅ **PASS** — banner read "Restoration Mode Active · Reason: PITR_RESTORE · Agent config version: 109 · DB config version: 106". Full chain captured in OM log between 14:05:30Z and 14:06:00Z: `ReconciliationOrchestrator` collected metadata from 1/1 agents → selected canonical config v109 → dispatched `CONFIG_UPLOAD AgentJob` → received 26,840 bytes → `ReconciliationConfigSvc` merged v109 into published v106 → bumped to v109 to prevent re-trigger → `GroupDao.tryClearRestorationMode` cleared the group flag → `RestorationModeSvc.enqueuePreExitSnapshots` queued on-demand snapshots for both poRepSet and the poShardClust cluster → `RestorationModeSvc.exitRestorationMode` exited cleanly → 2 fresh poRepSet snapshots completed at 14:07:06Z + 14:10:03Z (both `completed=true`, 52 fileIds each), shard + config-server snapshots at 14:12 |
| 4b | Restoration-mode enter/exit via API | _TBD_ | _TBD_ | n/a | _TBD_ — covers PR #754's public-API entry path; the auto-detect path (4) exercises the same `RestorationModeSvc` + `GroupDao` code |
| 5 | Feature-flag-disabled regression check | _TBD_ | _TBD_ | _TBD_ | _TBD_ |

#### Tier 2

| # | Probe | Outcome |
|---|---|---|
| 6 | oplog-meta rollback / PITR picker reject | _TBD_ |
| 7 | appdb rollback / 404 on forgotten snapshot | Implicitly observed in Probe 4 setup — appdb rollback succeeded, restoration-mode flow handled cleanly. Formal probe still _TBD_ |
| 8 | Config-encryption-at-rest | _TBD_ |
| 9 | Reconciliation TTL eviction | _TBD_ |

#### Run 1 operational notes (gotchas for future runs)

1. **`OM_DONT_RUN_MIGRATIONS=1` is required to start v8.0 OM against a main-built appdb.** The migration framework refuses cross-branch `NoMigrationPathException` because the recorded last-application-version hash (main-era `875e8d9f7ef…`) isn't in v8.0's compiled-in migration list. The env var skips the migration runner entirely — safe here because main's migrations are a strict superset of v8.0's. JVM flag `-Dmms.migration.path.verify=false` did **not** work — the migration runner is invoked as a separate pre-step and doesn't pick up bazel `--jvm_flag`.

2. **AppVersionPinger WARN spam every 10s — cosmetic, ignorable.** Same root cause as #1: appdb has the main commit hash, JVM is v8.0 commit hash. Logs `Detected a version mismatch between the application (875e…) and the Management HTTP Service (4c3e…)`. WARN level, no behavior gate, no impact on probes.

3. **Stale `mongoshTemplate.version` in appdb caused a brief DownloadMongosh failure loop at first agent start.** OM Backup project's automation config had `mongoshTemplate.version="2.6.0"` (carried over from main era), Topology Visualizer project had `"2.8.1"` (updated). Once v8.0 OM republished its config the value moved to `2.6.0` everywhere (v8.0's `conf-hosted.properties` `mongosh.version=2.6.0`). The download itself was blocked by macOS Gatekeeper killing the unsigned binary (`signal: killed`); after a couple of retries the download landed and the symlink check passed. Not blocking — but if you're on an Apple Silicon Mac and see early `DownloadMongosh` errors, ride them out for a minute before investigating.

4. **Agent config `local.config` must reference the correct project.** The 4 cherry-picked configs from `prakhar.dhama/om-backup-local` point at the "Topology Visualizer" project (`6a03286f…`); for "OM Backup" PoC use `mmsGroupId=69ef3ca159819e73bb7f8552` + the matching apiKey. The OM UI silently shows "no agent" / red status if you view the project the agent isn't reporting to — that *is* the bug if anyone tries to run the PoC fresh and sees nothing.

5. **`BounceStopIfUpWithForce` instead of `BounceStopIfUpWithForceKill`.** The v8.0 agent's restore step uses `BounceStopIfUpWithForce` (no `Kill` suffix) — a renaming from older main builds. Worth noting if you're grep-comparing logs across branches.

6. **OM Backup Phase 1 changed the post-restore snapshot behavior.** `RestorationModeSvc.enqueuePreExitSnapshots` (PR #555) queues on-demand snapshots for every RS + sharded cluster in the group right before exiting restoration mode. Observed in Run 1: poRepSet got one auto-snapshot at 14:07:06Z (~1 min after exit) and a second at 14:10:03Z (likely the 15-min scheduled), both complete with 52 fileIds. The two-shard cluster `poShardClust` got per-shard + config-server snapshots a few minutes later. This is **new behavior on v8.0** that didn't exist pre-Phase-1; if you're comparing snapshot inventories to a pre-backport baseline, expect 2× the cadence in the 5-min window after any restoration-mode exit.

---

## Sign-off criteria

- All Tier-1 probes match expected outcomes
- Any Tier-2 failure has a triage entry (suspect backport, suspect env, or known divergence)
- Result log committed back to this file
- Outcome summarised in the OM Backup Phase 1 release-readiness doc
